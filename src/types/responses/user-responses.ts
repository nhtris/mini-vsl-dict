export interface LoginResponseData {
  username: string;
  firstName: string;
  lastName: string;
  accessToken: string;
  refreshToken: string;
}

export interface UserInfoResponseData {
  username: string;
  firstName: string;
  lastName: string;
}
